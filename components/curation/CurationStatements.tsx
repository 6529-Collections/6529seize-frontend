import { GeneralFilter } from "../../helpers/filters/Filters.types";

export default function CurationStatements({
  filters,
  setFilters,
}: {
  readonly filters: GeneralFilter;
  readonly setFilters: (filters: GeneralFilter) => void;
}) {
  return <div>123</div>;
}
