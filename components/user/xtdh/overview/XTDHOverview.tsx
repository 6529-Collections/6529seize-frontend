"use client";

import XTDHCard from "../ui/XTDHCard";
import XTDHStat from "../ui/XTDHStat";
import type { Summary } from "../types";

import { formatNumberWithCommasOrDash } from "@/helpers/Helpers";

const asValue = (v: number | null, suffix = "") =>
  v == null ? "—" : `${formatNumberWithCommasOrDash(Math.floor(v))}${suffix}`;

export default function XTDHOverview({ summary }: { readonly summary: Summary }) {
  return (
    <div className="tw-grid tw-grid-cols-1 lg:tw-grid-cols-5 tw-gap-4">
      <div className="lg:tw-col-span-2 tw-flex tw-flex-col tw-gap-4">
        <XTDHCard title="TDH Overview">
          <div className="tw-grid tw-grid-cols-2 tw-gap-4">
            <XTDHStat label="Base TDH Rate / day" value={asValue(summary.baseRatePerDay)} />
            <XTDHStat
              label="xTDH Multiplier"
              value={summary.multiplier == null ? "—" : `× ${summary.multiplier}`}
            />
            <XTDHStat label="xTDH Rate / day" value={asValue(summary.xtdhRatePerDay)} />
            <XTDHStat label="Total TDH Rate / day" value={asValue(summary.totalRatePerDay)} />
          </div>
          <div className="tw-text-iron-400 tw-text-xs tw-mt-3">
            TDH = Base TDH + xTDH. Base TDH is generated by Memes & Gradients; xTDH is assignable.
          </div>
        </XTDHCard>

        <XTDHCard title="xTDH Capacity">
          <div className="tw-flex tw-flex-col tw-gap-3">
            <div className="tw-flex tw-justify-between tw-text-sm">
              <span className="tw-text-iron-300">Allocated (outgoing)</span>
              <span className="tw-text-iron-100">{asValue(summary.allocatedRatePerDay, " / day")}</span>
            </div>
            <div className="tw-h-2 tw-rounded tw-bg-iron-800">
              <div className="tw-h-2 tw-rounded tw-bg-primary-500" style={{ width: "0%" }} />
            </div>
            <div className="tw-flex tw-justify-between tw-text-sm">
              <span className="tw-text-iron-300">Incoming xTDH</span>
              <span className="tw-text-iron-100">{asValue(summary.incomingRatePerDay, " / day")}</span>
            </div>
          </div>
        </XTDHCard>
      </div>

      <div className="lg:tw-col-span-3 tw-flex tw-flex-col tw-gap-4">
        <XTDHCard title="What is xTDH?">
          <div className="tw-text-iron-300 tw-text-sm tw-space-y-2">
            <p>
              Base TDH is generated by Memes & Gradients and is predictable. xTDH is a multiplier-based
              capacity you can allocate to external targets.
            </p>
            <p>
              Allocating xTDH reduces your own accrual; totals remain capped by Base TDH. Grants are
              reversible and adjust pro‑rata if your Base TDH changes.
            </p>
          </div>
        </XTDHCard>
      </div>
    </div>
  );
}
