# Worktree Sync - COPY MODE
# Files/folders listed here will be COPIED once when worktree is created
# Each worktree gets its own independent copy
#
# Format:
#   path/to/file         - Single file
#   path/to/directory/   - Directory (trailing slash)

# Environment files
.env.development
