# Run log

## 2026-08-04 start

- Created a clean frontend worktree from exact `origin/main`
  `33f057f5003ed62269f2c6cca03dd1fdd40278e1`.
- Read root operating rules and the repository's autonomous-manager, PR,
  design, WCAG, i18n, React Doctor, Sonar, and docs skills and standards.
- Opened Museum source PR #22 at signed head `84175969cdfb`; source CI began on
  the 230-entry candidate manifest.
- First source review found a valid source-register reconciliation gap. The
  correction adds the verified Centre Pompidou _Icône_ record, reconciles every
  public manuscript, and tightens the source-count invariant before a new
  signed source head is pushed.

## 2026-08-04 governed source merge

- Corrected Tate's archive chronology, separated Rhizome's May subscription
  closure from its June platform-replacement plan and current service notice,
  fixed the Met grammar defect, and hardened external-link validation.
- Exact source head `25d13e494e4dbc8b22d0ff3737ca75531346da7b`
  passed 125 repository tests with one intentional Windows named-pipe skip,
  bootstrap validation, full Museum validation, fetch guard, Casey dossier and
  diligence checks, deterministic Ubuntu and Windows CI, CodeRabbit, and a
  6529bot follow-up with no new findings.
- All seven inline review threads were resolved with explicit implementation
  or disposition notes. Self-referential future-commit fields and retroactive
  `supersedes` records for unreleased drafts were not invented; the website
  supplies the immutable edition URL from verified publication identity.
- PR #22 merged as canonical source commit
  `f5080e1873a3b86280c5a92e1fbe6cbd7fea38a4` with 230 manifest entries,
  SHA-256
  `sha256:7ae561a27b5c3494d3bc81035af506ba5c49501ebb5c73a5535a3a2898c1b416`,
  and Keccak-256
  `0xe71d1d744b2bccf1e2c724ab907a5bcc8e53bbf9befdc8f93b21ff89e76dd93c`.
- The frontend publication core now requires all sixteen study manuscripts
  atomically and caps aggregate document bytes while fetching documents in
  bounded batches. Core focused validation is 41/41 tests green.

## 2026-08-04 frontend local qualification

- The exact-source probe resolved canonical Museum main
  `f5080e1873a3b86280c5a92e1fbe6cbd7fea38a4`, all 230 manifest entries,
  SHA-256
  `sha256:7ae561a27b5c3494d3bc81035af506ba5c49501ebb5c73a5535a3a2898c1b416`,
  Keccak-256
  `0xe71d1d744b2bccf1e2c724ab907a5bcc8e53bbf9befdc8f93b21ff89e76dd93c`,
  42 atomic public documents, 14 profiles, the study, and the source register.
- Local validation passed: 79 Museum suites / 282 tests, 10 focused suites /
  180 tests, changed lint, application typecheck, Jest ratchet, Playwright
  typecheck, documentation-link validation, help and agent-file sync, E2E
  manifest drift check, React Doctor 100/100, a 390.9-second initial production
  build, and a 414.9-second exact post-polish production build.
- The first browser-pack start timed out before a test because the shared local
  API on port 3000 was not running. The unchanged application started against
  the public read-only production API endpoint used by hosted CI.
- The next browser pass exposed the local Analytics SDK's known failed-fetch
  noise. The Museum test now uses the same exact test-only allowlist already
  established by the critical-shell suite; runtime behavior was not changed.
- The first complete product pass found a valid omission: institutional
  profiles lacked an onsite path to the primary-source register. Every profile
  now ends with that scholarly route, covered by the route suite.
- The exact 32-case read-only pack then passed all 16 routes on desktop and
  exact 390 x 844 mobile viewports. It checked one H1, source commit and path,
  contribution links, safe external links, focus visibility, console/page
  errors, and horizontal overflow.
- Owning pixel review covered the study, Serpentine Arts Technologies profile,
  source register, Stories & Research entry, and numbered profile directory at
  1440 x 1000 and 390 x 844. All sampled pages remained within the viewport.
  Mobile publication metadata was polished into a vertical colophon so
  separators no longer dangle at line ends.

## 2026-08-04 frontend review

- Ready PR #3569 opened from exact signed head `94c1ef3f9c1a3fe0f3ee9b20e60deadd3a01dae3`.
- The first hosted readback found that the commit carried an SSH signature but
  omitted the repository's DCO trailer. This is a commit-metadata defect, not a
  product, content, or validation finding. The head is being replaced with an
  otherwise identical signed and signed-off commit before substantive review.
- The superseded head's installed-app job identified three module-internal
  declarations exposed from their files. Their `export` modifiers were removed;
  the declarations, runtime graph, publication contract, copy, and pixels are
  unchanged. Fresh exact-head Knip is authoritative.
- The general and i18n reviews identified three valid fail-closed boundaries:
  the assembler and renderer used separate title parsers, shaped ISO dates were
  not checked against the calendar, and display formatting bypassed the shared
  locale helper. One exact heading parser now serves both layers; invalid dates
  fail projection; both-absent and invalid-date cases are covered; and the
  shared formatter renders validated dates. The document loader also uses a
  bounded worker pool instead of windowed batches.
- Sonar's 98 duplicated new lines were all repeated profile-contract literals
  in `institutionalPractice.ts`. A typed local constructor now states each
  institution once while preserving the same closed ids, paths, titles, order,
  and runtime records.
- Review-follow-up validation passed: 68 Museum suites / 195 tests, focused
  publication and route validation 43/43, changed lint and typecheck (1,264
  files), Knip with no findings, React Doctor 100/100, and the portable diff
  check. No visitor-facing copy, layout, link, or styling changed in this pass.
