interface:
  display_name: "Deploy 6529"
  short_description: "Route exact releases by effective lane state."
  default_prompt: "Use $deploy-6529 to route, deploy, validate, and recover an exact 6529 frontend, backend, or coupled release."
