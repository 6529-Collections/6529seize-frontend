# Release Bus v2 frontend-only activation proof

This file intentionally changes no runtime behavior. It gives the bounded
operator-owned activation and performance proof one exact green merge-tree
while its deploy plan selects only the frontend application.

- Proof ID: `frontend-only-activation-1`
- Deploy plan: `frontend-only`
- Release notes: `disabled` for this internal operational proof
