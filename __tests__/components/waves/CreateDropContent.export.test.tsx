import CreateDropContent from '../../../components/waves/CreateDropContent';

describe('CreateDropContent export', () => {
  it('exports a React component', () => {
    expect(typeof CreateDropContent).toBe('object');
  });
});
