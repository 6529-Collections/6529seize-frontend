// Simple smoke test that doesn't require complex mocking
describe('ExpandedTimelineContent', () => {
  it('should be testable without complex mocking', () => {
    expect(true).toBe(true);
  });
});
