# PR Report: Network Nerd Mobile Layout

PR: https://github.com/6529-Collections/6529seize-frontend/pull/2775
Branch: codex/network-nerd-responsive -> main
Related PRs: None
Mode: staging-deploy

## Summary
Fixes the Network Nerd page on phone-width screens by preventing the filter controls, focus tabs, and dense leaderboard table from colliding or squeezing into unreadable columns.

## What Changed
- Stacked the Network Nerd toolbar controls on narrow screens.
- Converted the focus tabs into full-width mobile tap targets.
- Added explicit horizontal-scroll sizing for the leaderboard table so columns keep readable spacing.

## Frontend Impact
- Routes/views: `/network/nerd`, `/network/nerd/cards-collected`, `/network/nerd/interactions`.
- Components: `components/leaderboard/Leaderboard.module.scss`.
- Generated API/client: Not affected.
- User-visible behavior: mobile users should see separated controls and a horizontally scrollable leaderboard instead of overlapped toolbar/table text.

## Config / Env
None.

## Deployment
Frontend staging deploy pending. This PR is intended to merge through the normal `1a-staging` frontend path after PR readiness gates.

## Release Notes
Improves mobile responsiveness for Network Nerd so leaderboard filters, view tabs, and table columns remain usable on phone-width screens.

## Verification
- `./bin/6529 run lint:changed`
- `./bin/6529 run typecheck:changed`
- `./bin/6529 run react-doctor:diff`
- Sass compile check for `components/leaderboard/Leaderboard.module.scss`

## Risks / Follow-Ups
- Local full build did not reach a useful terminal result in this worktree after long silent runs; PR CI and staging deployment remain the full build/runtime validation gates.
- `./bin/6529 run check:changed` reached the repo-wide Knip deadcode scan and failed on existing unused exports/files outside this CSS-only change.
