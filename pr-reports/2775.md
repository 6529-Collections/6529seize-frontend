# PR Report: Network Nerd Mobile Layout

PR: https://github.com/6529-Collections/6529seize-frontend/pull/2775
Branch: codex/network-nerd-responsive -> main
Related PRs: None
Mode: staging-deploy

## Summary
Fixes the Network Nerd page on phone-width screens by preventing the filter controls, focus tabs, and dense leaderboard table from colliding or squeezing into unreadable columns.

## What Changed
- Stacked the Network Nerd toolbar controls on narrow screens.
- Converted the focus tabs into full-width, high-contrast mobile tap targets with scoped mobile styles that survive the base focus-button cascade.
- Added mobile-only horizontal-scroll sizing and a `>>` cue for the leaderboard table so phone users can discover the scroll affordance without changing desktop table layout.

## Frontend Impact
- Routes/views: `/network/nerd`, `/network/nerd/cards-collected`, `/network/nerd/interactions`.
- Components: `components/leaderboard/Leaderboard.module.scss`.
- Generated API/client: Not affected.
- User-visible behavior: mobile users should see separated controls and a hinted horizontally scrollable leaderboard instead of overlapped toolbar/table text; desktop keeps the normal fitted table layout.

## Config / Env
None.

## Release Notes
Improves mobile responsiveness for Network Nerd so leaderboard filters, view tabs, and table columns remain usable on phone-width screens.

## Risks / Follow-Ups
- Corrective staging redeploy is pending for the mobile-only table scroll scope.
- Local full build did not reach a useful terminal result in this worktree after long silent runs; staging deployment and manual review remain the full runtime validation gates.
- `./bin/6529 run check:changed` reached the repo-wide Knip deadcode scan and failed on existing unused exports/files outside this CSS-only change.
