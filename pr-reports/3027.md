# PR Report: Restore gated Drop Forge sidebar row

PR: https://github.com/6529-Collections/6529seize-frontend/pull/3027
Branch: codex/restore-drop-forge-nav -> main
Related PRs: None

## Summary
Restores `Drop Forge` as the standalone sidebar/app-drawer row after `About` while preserving the previous `canAccessLanding` permission gate. The route remains hidden for wallets that cannot access `/drop-forge`.

## Changes
- Reinstates the gated top-level `Drop Forge` row in the web sidebar and app drawer.
- Keeps `/drop-forge` and nested `/drop-forge/*` active state on the restored web sidebar row.
- Updates navigation and Drop Forge help/docs so user-facing guidance matches the restored row placement.
- Adds regression coverage for hidden and visible Drop Forge sidebar states.

## Risks / Follow-Ups
This is a sensitive navigation visibility fix. Review should focus on confirming the gate remains `useDropForgePermissions().canAccessLanding` and that no public wallet state can see the row.
