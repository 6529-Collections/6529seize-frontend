# PR Report: Mobile Floating Dock Refinements

PR: https://github.com/6529-Collections/6529seize-frontend/pull/2865
Branch: codex/mobile-floating-dock-refine -> main
Related PRs: None
Mode: staging-deploy

## Summary

Refines the mobile floating dock after the initial rollout so list routes consistently use the floating treatment, detail routes get their full composer space back, and active-state transitions feel less jumpy.

## What Changed

- Widened the expanded floating dock while keeping the compact dock size unchanged.
- Removed the pending-route highlight so the active indicator remains on the current route until it animates to the next active item.
- Switched waves, messages, and notifications list routes away from the fixed dock and onto the floating dock.
- Added notification bottom spacing and reversed notification scroll compaction to match the bottom-starting feed.
- Hid the dock on wave and message detail routes while keeping the existing slide-away hidden state.
- Moved the active indicator motion boundary to the dock-level `LayoutGroup` so the active background can animate between items.
- Scoped element-scroll compaction to marked mobile stream scroll containers and batched all scrolled targets per animation frame.

## Frontend Impact

- Routes/views: `/waves`, `/messages`, and `/notifications` use the floating dock; `/waves/[id]` and `/messages/[id]` hide it.
- Components: Updates `BottomNavigation`, `NavItem`, `AppLayout`, mobile waves/messages scrollers, reverse drop containers, and notifications reverse-list spacing.
- Generated API/client: Not affected.
- User-visible behavior: Mobile dock sizing, active-state animation, route visibility, and notification bottom spacing are updated.

## Validation

- `seize run test:no-coverage -- __tests__/components/navigation/BottomNavigation.test.tsx __tests__/components/navigation/NavItem.test.tsx __tests__/helpers/navigation.helpers.test.ts __tests__/components/layout/AppLayout.test.tsx`
- `seize run lint:changed`
- `seize run typecheck:changed`
- `seize exec react-doctor . --project 6529seize --verbose --offline --diff=main`
- `codex-diff-check`

## Config / Env

None.

## Release Notes

Improves the mobile floating dock rollout by using it on stream list pages, removing the transient pending highlight, and hiding it on wave/message detail pages.
