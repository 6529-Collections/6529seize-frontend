# PR Report: Frontend Knip Cleanup

PR: https://github.com/6529-Collections/6529seize-frontend/pull/2860
Branch: codex/knip-cleanup -> main
Related PRs: None
Mode: staging-deploy

## Summary
Turns Knip dead-code detection into a clear frontend PR CI signal and cleans up the current actionable Knip findings so the new gate starts from a clean baseline.

## What Changed
- Added a dedicated Knip dead-code step to `App PR CI / Installed app checks` after dependency installation.
- Tuned `knip.jsonc` to keep raw Knip output focused on actionable application dead-code findings instead of broad test and mock file noise.
- Removed unused frontend files, stale compatibility barrels, obsolete helper exports, and unused local-only helpers reported by Knip.
- Updated tests and imports to use canonical modules directly for Meme Lab card content, Rememes types, YouTube URL parsing, and video template types.
- Kept narrow Knip exceptions for generated/config files and production helpers that are intentionally exported for focused tests.

## Frontend Impact
- Routes/views: Not affected.
- Components: Removes unused component/barrel code and unused public helper exports across existing frontend modules.
- Generated API/client: Not affected.
- User-visible behavior: No intended behavior change.

## Config / Env
None.

## Release Notes
Improves frontend CI dead-code coverage by adding a dedicated Knip check and cleaning up the existing Knip findings.
