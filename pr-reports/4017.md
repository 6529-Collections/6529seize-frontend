# PR Report: Increase test typecheck heap

PR: https://github.com/6529-Collections/6529seize-frontend/pull/4017
Branch: `agent-prxt/ci-test-typecheck-memory` -> `main`

The “Typecheck Jest, Playwright, and test helpers” step in
`.github/workflows/app-pr-ci.yml` now sets
`NODE_OPTIONS: --max-old-space-size=6144`, allowing 6144 MiB of Node.js old-space
for the test-code typecheck process.

The environment override applies only to this step. Its condition and run command,
`package.json`, and local commands remain unchanged. No application, backend, or
desktop behavior changes.
