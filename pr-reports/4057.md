# PR Report: Frontend E2E maintenance

PR: https://github.com/6529-Collections/6529seize-frontend/pull/4057
Branch: agent-prxt/frontend-e2e-maintenance -> main

## Summary

Require frontend implementation work to inspect and maintain affected E2E coverage in the same PR, even when browser execution is unavailable or CI is not awaited.

## What Changed

- Add an explicit E2E maintenance requirement to the root agent instructions, including specs, shared selectors, fixtures, helpers, and pack/CI selection.
- Allow changed behavioral expectations only when justified by the user's requested behavior, with that rationale in the PR. Preserve unaffected guarantees and fix product regressions instead of adapting tests to implementation mistakes.
- Connect PR preparation to that requirement, with clear reporting of reviewed or updated coverage versus actual execution.
- Point contributors to PR workflow/planner selection and clarify that local browser commands remain subject to server-start authorization.
- Give the browser-test matrix a static display name so skipped jobs show "App check / Browser tests" instead of an unexpanded expression. Test selection and the required aggregate check are unchanged.

## Release Notes

Frontend contributors must account for affected browser coverage before PR handoff. Skipped browser checks have a readable name. Application behavior, execution permissions, and deployment gates are unchanged.
