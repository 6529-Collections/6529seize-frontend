# PR Report: Wallet auth session v2 contract and client hardening

- GitHub PR: https://github.com/6529-Collections/6529seize-frontend/pull/2556
- Branch: `codex/wallet-security-hardening`
- Target: `main`
- Related backend PR: https://github.com/6529-Collections/6529seize-backend/pull/1616

## Task Summary

Auth v2 frontend hardening for wallet session v2 contracts, generated API models, structured wallet signatures, connection sharing UI flow, websocket/session compatibility, and the replayed local v1-to-v2 migration prompt work from `/Users/ppan/Desktop/6529git/6529seize-frontend`.

## Frontend Change Summary

- Updated frontend auth/session handling for auth v2 web/native compatibility.
- Added structured wallet signature generation paths; rollout/enforcement is now driven by backend settings rather than frontend env flags.
- Added connection sharing acceptance flow and associated auth/session utilities.
- Updated generated API models/client surfaces from backend auth v2 contract changes.
- Addressed generated-model static-analysis feedback, including unused generated imports.
- Replayed the local unpushed v1-auth migration logic onto the clean PR branch:
  - valid v1 JWT sessions now prompt users to upgrade to session v2 instead of immediately logging out;
  - users can defer the upgrade prompt for 2-hour reminder intervals while temporary access remains;
  - each wallet gets up to 72 hours from first prompt, capped by backend `/api/settings.auth.session_v2_migration_deadline`;
  - non-signable active/shared auth connections show a reshare/update-required prompt instead of a sign button;
  - disconnected browser sessions still show the upgrade sign flow, with an additional shared-connection reshare note;
  - temporary access time is displayed in total hours, or minutes when one hour or less remains;
  - expired migration windows clear active auth and invalidate cached state.
- Added session-v2 marker persistence so refreshed/logged-in v2 sessions are distinguished from legacy v1 auth.
- Allows cookie-backed v2 web auth against trusted cross-origin API endpoints, specifically `https://6529.io` calling `https://api.6529.io` and staging equivalents, when `WEB_SESSION_CREDENTIAL_API_ORIGINS` is configured.
- Switched websocket auth failure/timeout close codes to app-private close codes.
- Removed browser-rejected Permissions-Policy directives from security headers.

## Frontend Impact Summary

- Routes/views/components changed: connection sharing acceptance route, auth providers/context, v1-auth migration sign modal, wallet setup/adapter behavior, header/sidebar auth display paths, drop/signature-related flows.
- Generated models/API client changes: auth nonce/session/login/logout/refresh/connection-share generated models and API client surfaces.
- User-visible behavior changes: users can participate in the auth v2 wallet session and connection sharing flows once backend/env gates are enabled; legacy v1 sessions are prompted to migrate only when backend auth settings include a deadline or strict mode, can defer reminders while allowed, and are removed after their effective migration deadline.

## OpenAPI Refresh

- Re-check result: yes, this frontend feature depends on backend OpenAPI changes for auth settings, auth session/native refresh models, and connection sharing contracts.
- Backend branch used for refresh: `codex/wallet-security-hardening`.
- Commands run: `bash scripts/refresh-api.sh codex/wallet-security-hardening` and `./bin/6529 run generate`.
- Refresh result: `openapi.yaml` and `generated/` changed. The refreshed backend branch spec includes the auth v2 settings/session surfaces and also current backend branch additions for `/v2/drops/{id}/replies`, `/media/resolve`, and decentralized-media resolver models.
- Generated files changed/added: `generated/models/ApiAuthSettings.ts`, `generated/models/ApiSessionNativeResponse.ts`, `generated/models/ApiSessionRefreshNativeRequest.ts`, `generated/models/ObjectSerializer.ts`, `generated/models/ApiDecentralizedMediaProtocol.ts`, `generated/models/ApiDecentralizedMediaResolution.ts`, `generated/models/ApiMediaResolveRequest.ts`, and `generated/models/ApiMediaResolveResponse.ts`.

## Env And Config

| Name                                  | Owner                                | Status                                | Requiredness                                                       | Purpose                                                                                                                                         | Default/Fallback                                   | Recommended staging value shape                                                                                           | Location                              | Rollout / failure notes                                                                                                                                                                        |
| ------------------------------------- | ------------------------------------ | ------------------------------------- | ------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------- | ------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `AUTH_SESSION_HASH_SECRET`            | Backend API dependency               | New/recommended                       | Strongly recommended                                               | Dedicated backend secret for hashing session and refresh token material                                                                         | Backend falls back to existing JWT secret if unset | Dedicated high-entropy secret, different from JWT secret                                                                  | Backend secret manager/SSM/Lambda env | No FE env needed. If not set, auth still works, but session hash isolation is reduced.                                                                                                         |
| `WEB_SESSION_CREDENTIAL_API_ORIGINS`   | Frontend                             | New FE env                            | Required for direct web-to-API v2 cookie auth                      | Allows browser web auth utilities to send `credentials: "include"` to trusted cross-origin API origins                                         | Empty list                                          | Production: `https://api.6529.io`; staging: `https://api.staging.6529.io`                                                   | FE build/runtime env                  | Put API origins here, not web origins. If missing, FE intentionally refuses cross-origin cookie refresh/logout/login attempts.                                                                  |
| `AUTH_WEB_CREDENTIAL_ORIGINS`          | Backend API dependency               | New backend env                       | Required for direct web-to-API v2 cookie auth                      | Backend allowlist for credentialed CORS on v2 web-cookie auth routes                                                                           | Empty list                                          | Production: `https://6529.io`; staging: `https://staging.6529.io`                                                           | Backend Lambda/API env                | Put web origins here, not API origins. General API CORS remains wildcard/non-credentialed for external API clients.                                                                            |
| `AUTH_WALLET_CHAIN_ID`                 | Backend API dependency               | Optional backend env                  | Optional                                                           | Pins structured wallet-auth verification to the configured auth chain                                                                          | `1`                                                | `1` or unset unless intentionally testing another chain                                                                    | Backend Lambda/API env                | Prevents clients from choosing the accepted auth chain through request params.                                                                                                                 |
| `AUTH_STRUCTURED_SIGNATURES_REQUIRED` | Backend API and `/api/settings.auth` | Backend-controlled strict flag        | Optional initially; final strict-mode flag                         | Backend rejects legacy/unstructured signatures where structured signatures are expected and exposes `auth.structured_signatures_required` to FE | `false` when unset                                 | Keep `false` for backend-first deploy and deadline rollout; set `true` only after FE/mobile/external clients are verified | Backend Lambda/API env                | FE reads this from settings. If true, v1-auth sessions are no longer deferrable. If set too early, old clients can break.                                                                      |
| `SESSION_V2_MIGRATION_DEADLINE`       | Backend API and `/api/settings.auth` | Backend-controlled migration setting  | Optional; set after FE deploy when migration campaign should begin | Global hard cutoff for FE v1-auth temporary access                                                                                              | `null` in settings when unset                      | ISO datetime with timezone, for example `2026-06-25T00:00:00.000Z`                                                        | Backend Lambda/API env                | No FE env needed. If unset, FE stays silent unless strict mode is true. Effective cutoff is `min(first_prompt + 72h, backend_deadline)`. If set in the past, v1 auth is cleared on validation. |
| `AUTH_CONNECTION_SHARING_DISABLED`    | Backend API dependency               | Backend-controlled kill switch        | Optional; set only when connection sharing must be disabled        | Disables transfer-code based connection sharing endpoints when explicitly true                                                                  | Enabled unless explicitly `true`                   | Leave unset/`false` for staging validation                                                                                 | Backend Lambda/API env or SSM         | If true, normal FE auth works but the Connection share tab will be unavailable because connection-share creation fails.                                                                         |
| `AUTH_LEGACY_WS_QUERY_TOKEN_ENABLED`  | Backend API websocket dependency     | Backend-controlled compatibility flag | Optional; keep enabled during rollout                              | Allows legacy websocket query-token auth transport                                                                                              | Enabled unless explicitly `false`                  | `true` or unset until all websocket clients use the new path                                                              | Backend Lambda/API env                | Separate from structured-signature strict mode. If disabled early, older websocket clients can fail.                                                                                           |

Frontend env note: `AUTH_STRUCTURED_SIGNATURES_ENABLED` and frontend-owned `SESSION_V2_MIGRATION_DEADLINE` are no longer used. The FE emits structured signatures where supported and reads rollout state from `/api/settings.auth`. The only FE env added by this follow-up is `WEB_SESSION_CREDENTIAL_API_ORIGINS`, which is for trusted browser cookie auth to the API origin.

Backend-only deployment note: if the backend with the new `auth` settings object is deployed without deploying this FE, existing web clients continue working and ignore the new settings object. Set the backend deadline only after this FE is deployed.

## Validation

- OpenAPI refresh: `bash scripts/refresh-api.sh codex/wallet-security-hardening` passed after network approval, then `./bin/6529 run generate` passed.
- Local focused Jest: `./bin/6529 run test:no-coverage -- __tests__/components/auth/Auth.test.tsx __tests__/contexts/SeizeSettingsContext.test.tsx __tests__/services/wallet-signatures/structured-wallet-signatures.test.ts` passed (`3` suites, `48` tests).
- Local format after OpenAPI refresh: `./bin/6529 run format:uncommitted` passed.
- Local ESLint diff after OpenAPI refresh: `./bin/6529 run lint:diff` passed.
- Local changed-file TypeScript after OpenAPI refresh: `./bin/6529 run typecheck:changed` passed (`51` changed TypeScript files).
- Local whitespace check after OpenAPI refresh: `git diff --check` passed.
- Local focused Jest after trusted cross-origin cookie auth support: `./bin/6529 run test:no-coverage -- __tests__/services/auth/session-v2.utils.test.ts --runInBand` passed (`1` suite, `21` tests).
- Local changed-file lint after trusted cross-origin cookie auth support: `./bin/6529 run lint:changed` passed.
- Local changed-file TypeScript after trusted cross-origin cookie auth support: `./bin/6529 run typecheck:changed` passed (`55` changed TypeScript files).
- Local React Doctor after trusted cross-origin cookie auth support: `./bin/6529 run react-doctor:diff` exited `0` with score `100 / 100`.
- Local whitespace check after trusted cross-origin cookie auth support: `git diff --check` passed.
- Local `./bin/6529 run quality:changed` still fails on existing branch-wide Knip deadcode findings outside this auth utility follow-up.
- Local TypeScript before this follow-up: `pnpm exec tsc --noEmit -p tsconfig.typecheck.json --pretty false` passed.
- Latest PR checks before this follow-up update: DCO, CodeRabbit, Snyk, SonarCloud, and CodeQL `Analyze (actions)`, `Analyze (python)`, and `Analyze (javascript-typescript)` passed. One aggregate CodeQL workflow entry was skipped; checks should be reviewed again after publishing this follow-up.
- SonarCloud leak query before this update: zero open/confirmed new-code issues after Sonar cleanup.
- Review threads before this update: no unresolved actionable review threads remained; generated-model unused import noise should be treated as generated-code false positives if it reappears.

## Staging Deployment Summary

- Frontend staging deploy result: `None`; user explicitly requested no staging deployment.
- Frontend staging run link or ID: `None`

Planned staging order when approved:

1. Deploy backend first with `AUTH_STRUCTURED_SIGNATURES_REQUIRED=false` and `SESSION_V2_MIGRATION_DEADLINE` unset; old web ignores `/api/settings.auth`.
2. Set/confirm backend secrets and compatibility flags:
   - `AUTH_SESSION_HASH_SECRET=<dedicated high-entropy secret>`;
   - `AUTH_WEB_CREDENTIAL_ORIGINS=https://staging.6529.io` for staging or `https://6529.io` for production;
   - `AUTH_WALLET_CHAIN_ID=1` or unset unless intentionally testing another chain;
   - `AUTH_LEGACY_WS_QUERY_TOKEN_ENABLED=true` or unset;
   - `AUTH_CONNECTION_SHARING_DISABLED` unset/`false` unless intentionally disabling connection sharing.
3. Deploy backend `dbMigrationsLoop` first, then backend `api`.
4. Deploy frontend with:
   - production: `API_ENDPOINT=https://api.6529.io` and `WEB_SESSION_CREDENTIAL_API_ORIGINS=https://api.6529.io`;
   - staging: `API_ENDPOINT=https://api.staging.6529.io` and `WEB_SESSION_CREDENTIAL_API_ORIGINS=https://api.staging.6529.io`.
5. Smoke test auth session v2, legacy v1 migration prompts, shared connection reshare prompts, connection sharing, structured signing, and websocket/session refresh flows.
6. After the FE is live and v2 flows are verified, set backend `SESSION_V2_MIGRATION_DEADLINE=<ISO cutoff with timezone>` to start migration prompts without another FE release.
7. Only after FE/mobile/external clients are verified, consider setting backend `AUTH_STRUCTURED_SIGNATURES_REQUIRED=true`.

Backend migration details are in the backend PR doc `docs/auth/session-v2-migration-plan.md`.

## Smoke-Test Plan

1. Complete wallet login/session refresh/logout in staging web.
2. With backend `SESSION_V2_MIGRATION_DEADLINE` unset and `AUTH_STRUCTURED_SIGNATURES_REQUIRED=false`, seed or reuse a valid v1-auth wallet session and confirm web stays silent.
3. Set backend `SESSION_V2_MIGRATION_DEADLINE` to a future ISO datetime and confirm the upgrade modal appears with a 72-hour local window capped by that backend deadline.
4. Dismiss the upgrade prompt and confirm it remains dismissed until the 2-hour reminder interval elapses.
5. Validate a non-signable/shared v1-auth connection shows the reshare/update-required copy and no sign button.
6. Validate a v1-auth session past the effective deadline is kicked out by clearing active auth state.
7. Start connection sharing on web and redeem it from the native/mobile flow.
8. Create a signed action/drop that uses structured signature payloads and confirm backend acceptance.

## Release Notes

- Adds frontend/client compatibility for wallet auth session v2.
- Adds connection sharing UI/runtime support for web-to-native handoff.
- Adds v1-auth migration prompting with 2-hour reminders, 72-hour local grace, global deadline cap, and shared-auth reshare guidance.
- Adds structured wallet-signature client support and generated API model updates.
- Reads backend-owned auth rollout settings from `/api/settings.auth`; no frontend env is required for the migration deadline.
- Strict backend structured-signature enforcement must be coordinated after frontend/mobile clients are verified.

## Residual Risks And Follow-Ups

- Report commit may retrigger CI/bots; latest PR checks should be reviewed after this file is published.
- Repo-wide Knip dead-code findings remain outside the auth-specific validation surface.
- Human review is still required on the frontend PR at the last refresh.
