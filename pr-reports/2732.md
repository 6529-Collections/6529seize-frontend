# PR 2732 Report: Send explicit link preview visibility state

## PR
- GitHub PR: https://github.com/6529-Collections/6529seize-frontend/pull/2732
- Branch: `codex/link-preview-toggle-state`
- Target: `main`
- Related backend PR: https://github.com/6529-Collections/6529seize-backend/pull/1648

## Task Summary
Fix inconsistent hide/restore link-preview behavior in wave posts by sending the intended final visibility state and reconciling the optimistic UI with the server response.

## Frontend Change Summary
- Changed link-preview actions to call `onToggle(true)` for hide and `onToggle(false)` for restore.
- Optimistically writes the desired `hide_link_preview` state instead of flipping local state.
- Reconciles the optimistic store with the returned `ApiDrop.hide_link_preview`.
- Guards overlapping requests for the same drop across hook instances, with owner cleanup on unmount.
- Regenerated frontend API model for `ApiToggleHideLinkPreviewRequest`.

## Frontend Impact Summary
- Routes/views/components changed: wave post link-preview controls and action menus.
- Generated models/API client changes: new `ApiToggleHideLinkPreviewRequest`; OpenAPI updated for optional request body.
- User-visible behavior: hide and restore link-preview actions should immediately reflect the intended final state and stay consistent after websocket updates or refresh.

## Backend Impact Summary
- Requires backend PR 1648 for idempotent server-side behavior.
- New Lambdas/services created: None.
- Existing Lambdas/services changed: backend `api` in related PR.
- Existing Lambdas/services redeployed due to shared-code impact: None.
- API changes: optional `hide_link_preview` boolean request body.
- DB/entity/migration changes: None.
- Queue/event/media/edge/integration changes: None.

## Env/Config Changes
None.

## Validation
- Passed: `./bin/6529 run react-doctor:diff`
  - Note: existing component-size warning on `ChatItemHrefButtons`.
- Passed: `./bin/6529 run lint:diff`
- Passed: `./bin/6529 run typecheck`
- Passed: `./bin/6529 exec jest --runTestsByPath __tests__/components/ChatItemHrefButtons.test.tsx -t "renders and triggers preview toggle action from context" --silent --verbose=false --coverageReporters=none`
- Passed after bot cleanup: `./bin/6529 run react-doctor:diff`
- Passed after bot cleanup: `./bin/6529 exec eslint --config eslint.config.diff.mjs --no-warn-ignored --max-warnings=0 components/waves/drops/useDropLinkPreviewToggleControl.ts __tests__/components/waves/drops/useDropLinkPreviewToggleControl.test.tsx`
- Passed after bot cleanup: `./bin/6529 exec tsc --noEmit -p tsconfig.typecheck.json`
- Passed after bot cleanup: `./bin/6529 exec jest --runTestsByPath __tests__/components/waves/drops/useDropLinkPreviewToggleControl.test.tsx __tests__/components/waves/drops/WaveDropActionsMore.test.tsx __tests__/components/ChatItemHrefButtons.test.tsx -t "useDropLinkPreviewToggleControl|triggers restore link previews from the desktop menu|renders and triggers preview toggle action from context" --silent --verbose=false --coverageReporters=none`
  - Note: Jest reported a worker shutdown warning after passing the selected tests.
- Partial: `./bin/6529 exec jest --runTestsByPath __tests__/components/waves/drops/WaveDropActionsMore.test.tsx __tests__/components/ChatItemHrefButtons.test.tsx --silent --verbose=false --coverageReporters=none`
  - `WaveDropActionsMore` passed.
  - `ChatItemHrefButtons` still has existing overlay focus/menu failures unrelated to this change.
- Partial: `./bin/6529 run check:changed`
  - Reached quality checks but failed on existing repo-wide Knip deadcode findings unrelated to this change.
- CI state: GitHub PR checks passed on latest pushed head (`51bcb979a`): Analyze actions, Analyze JavaScript/TypeScript, Analyze Python, CodeQL, CodeRabbit, DCO, SonarCloud, and Snyk.

## Review And Security State
- Review bots: CodeRabbit reported no actionable comments on the reviewed implementation, then hit a temporary rate-limit notice for the final report/cleanup diff while the GitHub check still passed. 6529bot reported an unmount-cleanup gap and hook test gaps; fixed with owner cleanup for the per-drop lock and focused hook tests. Latest 6529bot follow-up on `0e58cccab51d` reported no new findings.
- SonarCloud: Passed with zero new issues.
- Snyk: Passed.
- CodeQL: Passed.
- DCO: Commit includes DCO signoff.
- Human review: Pending.

## Staging Deployment Summary
- Backend API/Lambda services deployed: backend `api` deployed first from staging merge `3bfa445fe2bf292bbe7844e04db9098b5baab499`; run https://github.com/6529-Collections/6529seize-backend/actions/runs/27745848411 succeeded.
- Frontend staging deploy result: frontend deployed from staging merge `9d3a3ae09232a4c93283d21f5c5142ef2c964608`; run https://github.com/6529-Collections/6529seize-frontend/actions/runs/27746328753 succeeded.

## Smoke-Test Evidence
- Frontend staging smoke passed: `GET https://staging.6529.io/waves` redirected to `/access` and returned HTTP 200 HTML after deploy.
- Backend staging health passed: `GET https://api.staging.6529.io/health` returned HTTP 200 with `status: "ok"`, DB `ok`, Redis healthy, and deployed commit `3bfa445fe2bf292bbe7844e04db9098b5baab499`.
- Manual user verification recommended on staging:
  - Find or create a staging wave post with a link preview.
  - Click "Hide link previews" and confirm the preview disappears immediately and remains hidden after refresh.
  - Click "Restore link previews" and confirm the preview reappears immediately and remains visible after refresh.

## Release Notes
- User-visible: hide/restore link-preview actions in wave posts now set the intended final state instead of depending on a fragile local toggle.
- Operational/deployment impact: deploy backend `api` first, then frontend staging.
- Compatibility: frontend request remains accepted by the old backend, but deterministic stale-click behavior depends on backend PR 1648.
- Rollout: backend first, frontend second.

## Residual Risks And Follow-Up
- Existing unrelated frontend deadcode findings and ChatItem overlay focus tests remain outside this change.
- Authenticated staging click-flow was not automated in this session; verification should be done with a staging account/profile that can author a wave post with a link preview.
