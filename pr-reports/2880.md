# PR Report: Float Mobile Waves Quick Vote Above Dock

PR: https://github.com/6529-Collections/6529seize-frontend/pull/2880
Branch: codex/waves-uncast-floating-overlay -> main
Related PRs: None

## Summary

This refines the mobile `/waves` bottom layout so the waves list remains a continuous page surface behind the floating dock, while the Uncast Power quick-vote control floats above the dock instead of occupying an in-flow footer band.

## What Changed

- Mobile waves now uses its scroll container as the visible route surface, matching the mobile messages pattern more closely.
- The waves scroll area has additional bottom scroll clearance for the quick-vote overlay plus floating dock.
- `MemesWaveFooter` gained a mobile floating mode while preserving the existing desktop/sidebar footer and collapsed pill behavior.
- Focused tests cover the mobile waves scroll target contract and the floating quick-vote footer layer.

## Frontend Impact

- Routes/views: mobile `/waves`.
- Components: `BrainMobileWaves`, `MemesWaveFooter`.
- Generated API/client: Not affected.
- User-visible behavior: the waves list continues behind the dock, and the Uncast Power box appears above the dock without a full-width black footer strip.

## Config / Env

No config or environment changes.

## Release Notes

- Mobile `/waves` now treats the floating dock like an overlay on top of the page, with Uncast Power shown as a floating control just above it.
