save-exact=true
allow-git=none
@6529-collections:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}
# The secure helper fetches with scripts disabled, then rebuilds the explicitly
# approved dependencies without passing the package token.
